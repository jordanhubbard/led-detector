# Note: OpenCV for macOS is installed via Homebrew
# Run: brew install opencv
platform :osx, '13.0'

target 'CameraEdgeDetector' do
  use_frameworks!
  
  # No pods needed - using Homebrew OpenCV
end
